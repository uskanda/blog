- article ||= current_article
- date = article.date
- url = article.url
- with_link ||= false
.title 
  a.date[rel="bookmark"]
    abbr
      = date.strftime("%Y/%m")
    = date.day 
    sub
      = date.strftime("%R")
  .container
    .subject
      - if with_link then
        = link_to article.title, article
      - else
        = article.title
    .tags
      - article.tags.each do |tag|
        .label.label-primary
          = tag
/    .socials
/      = partial :social_button_top, locals: {url: url}
  .clearfix
